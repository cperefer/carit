import { WelcomeScreen } from "@/screens/WelcomeScreen";
import { SafeAreaView } from "react-native";

export const App = () => {
  return (
    <SafeAreaView>
      <WelcomeScreen />
    </SafeAreaView>
  );
}