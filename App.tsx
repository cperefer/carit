import { QuestionScreen } from "@screens/QuestionScreen";
import { SafeAreaView } from "react-native";

export const App = () => {
  return (
    <SafeAreaView>
      <QuestionScreen />
    </SafeAreaView>
  );
}